<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Topics - <?php echo $category->getName(); ?></title>
    <link rel="stylesheet" href="public/style.css">
</head>

<body>
    <a href="index.php">← Volver al Foro</a> |
    <a href="index.php?c=user&profile=1">Ver Perfil</a> |
    <a href="index.php?c=user&logout=1">Cerrar Sesión</a>

    <h1>📁 <?php echo $category->getName(); ?></h1>
    <p><?php echo $category->getDescription(); ?></p>

    <h2>Temas:</h2>

    <?php
    $topics = TopicRepository::getTopicsByCategory($category->getId());
    if (empty($topics)) {
        echo "<p>No hay topics en esta categoría aún.</p>";
    } else {
        foreach ($topics as $topic) {
            echo "<div class='topic-item'>";
            echo "<h3><a href='index.php?c=topic&id=" . $topic->getId() . "'>" . $topic->getTitle() . "</a></h3>";
            echo "</div><br>";
        }
    }
    ?>

    <a href="index.php?c=topic&create=1&category=<?php echo $category->getId(); ?>">Crear nuevo topic</a>

</body>

</html>